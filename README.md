There is a conflict when using the default JOOQ Generator Strategy for the enum types 'yes_no_values' and 'yesno_values'.

This pre-compiled Generator Strategy dependency resolves the issue by renaming them to YesNoValues1 and YesNoValues2 respectively.
